import Vue from 'vue'
// import components from './icon.vue'
// Object.keys(components).forEach((key) => {
//     Vue.component(key, components[key])
// })
import icon from './icon.vue'
Vue.component('svgIcon',icon)